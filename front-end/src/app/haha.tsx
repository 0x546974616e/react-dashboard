import { ScrollView } from "react-native-gesture-handler";
import { ThemePreview } from "@app/components";

export default function() {
  return (
    <ScrollView>
      <ThemePreview/>
    </ScrollView>
  );
}
